# Example environment variables for integrating real AI services
# Copy to .env and fill values before running the server.

# PORT the server listens on (default 5173)
PORT=5173

# GROQ / Llama 70B endpoint (example placeholder)
# e.g. https://api.groq.ai/v1/models/llama-70b-instant/execute
GROQ_API_URL=
GROQ_API_KEY=

# OpenAI Realtime or other speech platform API key — for server-side token minting or proxying
OPENAI_API_KEY=

# A short-lived token secret for client sessions (optional)
SESSION_SIGNING_SECRET=replace-me
